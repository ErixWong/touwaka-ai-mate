# Round 12 变更日志

## 变更概述

本轮针对 `audit-round12.md` 指出的纯文档问题做收口修复，未涉及运行时代码。

1. **P1-1：修正 Round 11 验证记录口径**
   - 修复位置：`changelog_round11.md`
   - 修复内容：
     - 将“主文档全部修复”细化为“主文档已无未解释乱码残留”
     - 明确 `[缺字]` 仍可能作为历史引用、检索示例或审计说明出现，不能一律按“正文乱码未修复”解读
     - 修正验证记录中对 `audit-round08.md`、`audit-round11.md` 的结论，避免把“仍有匹配”误写成“仅剩原文引用且可忽略”

2. **P1-2：修正工作树范围对账数字**
   - 修复位置：`changelog_round11.md`、`README.md`
   - 修复内容：
     - 将工作树总数从 `19` 修正为 `20`
     - 将“其他任务文件数”从 `8` 修正为 `9`
     - 在 `README.md` 中补充显式公式：`11 + 9 = 20`，确保表格数量与 `git status --short` 一致

3. **P2-3：补充审计文档勘误规则**
   - 修复位置：`SELF-TEST.md`
   - 修复内容：
     - 新增“历史审计文档勘误规则”
     - 明确历史 `audit-round*.md` 原则上不直接重写正文判断
     - 允许最小必要的编码修复，但必须记录修复原因、范围和是否影响原结论

4. **P2-4：明确任务生命周期状态**
   - 修复位置：`README.md`
   - 修复内容：
     - 将当前状态更新为“已完成（Round 12 文档收口，待复审归档）”
     - 明确目录暂留 `active` 的原因是多轮审计收口，而不是仍在开发实现
     - 明确下一步应转入 `review/archived`

---

## 验证记录

### 1. 工作树数量对账

当前 `git status --short` 共 20 个路径，现已与 README 的分类表一致：

- 本任务：11 个
- 其他任务：9 个
- 合计：20 个

### 2. `[缺字]` 检索口径修正

本轮不再将 `[缺字]` 的所有匹配简单视为“正文乱码未修复”，而是分为两类：

1. **允许保留**：历史乱码原文引用、审计说明、检索命令示例
2. **不允许保留**：未解释的正文乱码或缺字

后续审计如继续使用 `[缺字]` 作为检索模式，必须同步说明属于哪一类匹配。

---

## 本轮修改文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `docs/tasks/active/.../README.md` | 修改 | 明确任务阶段与工作树数量对账公式 |
| `docs/tasks/active/.../SELF-TEST.md` | 修改 | 新增历史审计文档勘误规则 |
| `docs/tasks/active/.../changelog_round11.md` | 修改 | 修正验证口径与工作树对账数字 |
| `docs/tasks/active/.../changelog_round12.md` | 新增 | 记录本轮纯文档收口动作 |

---

## 当前状态

- **运行时代码阻断项**：无
- **文档阻断项**：已完成本轮收口修正，待复审确认
- **下一阶段**：复审通过后转入 `review/archived`

---

*生成时间：2026-06-27*
